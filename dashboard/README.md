# AI Trading Evolution Dashboard

Laboratory command center for the evolving agent population.

Run from the repository root with:

```bash
pip install streamlit
streamlit run dashboard/app.py
```

The dashboard is intentionally laboratory-only and does not place live orders.
